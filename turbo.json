{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"],
      "//": "BUILD_TARGET changes what the build PRODUCES (standalone or not), so it has to be part of the cache key — otherwise `pnpm bundle` can be handed a cached Vercel-shaped build with no standalone folder in it.",
      "env": ["BUILD_TARGET"]
    },
    "lint": {
      "dependsOn": ["^build"]
    },
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "clean": {
      "cache": false
    }
  }
}
